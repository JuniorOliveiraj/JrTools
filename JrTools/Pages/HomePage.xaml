<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Estilo para os cards principais -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
        </Style>

        <!-- Estilo para os painéis de indicadores no topo -->
        <Style x:Key="DashboardTileStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="MinWidth" Value="200"/>
        </Style>
    </Page.Resources>
    
    

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel Spacing="16">

                <!-- Título da Página -->
                <TextBlock Text="Dashboard" Style="{ThemeResource TitleTextBlockStyle}" Margin="8,0"/>

                <!-- Painéis de Indicadores -->
                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 1. Banco de Horas -->
                    <Border Grid.Column="0" Style="{StaticResource DashboardTileStyle}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Banco de Horas" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.8"/>
                            <TextBlock Text="42h 30m" Style="{ThemeResource HeaderTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- 2. Previsão de Saída -->
                    <Border Grid.Column="1" Style="{StaticResource DashboardTileStyle}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Previsão de Saída (Hoje)" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.8"/>
                            <TextBlock Text="17:45" Style="{ThemeResource HeaderTextBlockStyle}"/>
                        </StackPanel>
                    </Border>

                    <!-- 3. Economia Mensal -->
                    <Border Grid.Column="2" Style="{StaticResource DashboardTileStyle}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Potencial de Economia (Mês)" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.8"/>
                            <TextBlock Text="R$ 600,00" Style="{ThemeResource HeaderTextBlockStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Cards de Ferramentas -->
                <StackPanel Spacing="16" Margin="0,16,0,0">

                    <!-- Card: Calculadora de Horas -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Calculadora de Horas" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                            <Grid ColumnSpacing="24" RowSpacing="12" MaxWidth="500">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TimePicker x:Name="Entrada1TimePicker" Header="Entrada 1" Grid.Row="0" Grid.Column="0" ClockIdentifier="24HourClock"/>
                                <TimePicker x:Name="Saida1TimePicker" Header="Saída 1" Grid.Row="0" Grid.Column="1" ClockIdentifier="24HourClock"/>
                                <TimePicker x:Name="Entrada2TimePicker" Header="Entrada 2" Grid.Row="1" Grid.Column="0" ClockIdentifier="24HourClock"/>
                                <TimePicker x:Name="Saida2TimePicker" Header="Saída 2" Grid.Row="1" Grid.Column="1" ClockIdentifier="24HourClock"/>

                                <Button x:Name="CalcularHorasButton" Content="Calcular" Grid.Row="2" Grid.Column="0" Click="CalcularHorasButton_Click"/>

                                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <TextBlock Text="Total:" VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock x:Name="TotalHorasTextBlock" Text="00:00" VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" FontSize="18"/>
                                </StackPanel>

                                <InfoBar x:Name="CalculadoraInfoBar" Grid.Row="3" Grid.ColumnSpan="2" IsOpen="False" Severity="Error" Margin="0,12,0,0"/>

                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Card: Menus Rápidos -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Menus Rápidos" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <!-- TODO: Adicionar navegação para as páginas correspondentes -->
                                <Button Content="Página de Processamento"/>
                                <Button Content="Configurações"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
