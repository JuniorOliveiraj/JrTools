<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="20">
            <TextBlock Text="Biblioteca" FontSize="24" FontWeight="Bold"/>

            <TextBlock Text="Atualizações e downloads (35)" FontSize="14" Margin="0,10,0,5"/>
            <TextBlock Text="Baixe e instale atualizações de aplicativos clicando em 'Obter atualizações'" FontSize="12"/>

            <Button Content="Obter atualizações" HorizontalAlignment="Right" Margin="0,10"/>

            <ItemsControl x:Name="UpdatesList" Margin="0,20,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,10" Background="#1F1F1F" Padding="10" CornerRadius="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Ícone -->
                            <Image Source="{Binding Icon}" Width="40" Height="40" Margin="0,0,10,0"/>

                            <!-- Informações -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Nome}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="Gray"/>
                                <ProgressBar Value="{Binding Progresso}" Maximum="100" Height="4"
                                         Visibility="{Binding MostraProgresso}"/>
                            </StackPanel>

                            <!-- Botão -->
                            <Button Grid.Column="2" Content="⏸" Width="32" Height="32"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Page>
