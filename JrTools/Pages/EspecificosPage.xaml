<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.EspecificosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" >

    <Page.Resources>
        <!-- Estilo para os cards que se ajustam em grade -->
        <Style x:Key="ResponsiveCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="320"/> 
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- Novo estilo para o card de terminal, que ocupa a largura total -->
        <Style x:Key="FullWidthCardStyle" TargetType="Border" BasedOn="{StaticResource ResponsiveCardStyle}">
            <Setter Property="MaxWidth" Value="9999"/>
            <!-- Remove a limitação de largura máxima -->
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel Spacing="16">
                <!-- CABEÇALHO -->
                <TextBlock Text="Montagem de Ambiente Específico" Style="{ThemeResource TitleTextBlockStyle}"/>

                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <TextBlock Text="Projeto" VerticalAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <ComboBox x:Name="ProjetoComboBox" MinWidth="250" SelectionChanged="ProjetoComboBox_SelectionChanged"/>
                </StackPanel>

                <!-- Barra para exibir mensagens de erro/validação -->
                <InfoBar x:Name="ValidationInfoBar" IsOpen="False" Severity="Error" Margin="0,0,8,0"/>

                <Border Style="{StaticResource FullWidthCardStyle}">
                    <StackPanel Spacing="5">
                        <ItemsControl x:Name="ProcessosItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <!-- CARD 1 -->
                            <Border Style="{StaticResource ResponsiveCardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Opções de Compilação" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                    <ToggleSwitch x:Name="BaixarBinarioToggle" Header="Baixar binário atualizado"/>
                                    <ToggleSwitch x:Name="CriarAtalhoToggle" Header="Criar atalho para pasta de compilação" IsOn="True"/>
                                    <ToggleSwitch x:Name="CompilarEspecificosToggle" Header="Compilar Específicos"/>
                                    <ToggleSwitch x:Name="CriarAplicacaoIISToggle" Header="Criar aplicação IIS" IsOn="True"/>
                                    <ToggleSwitch x:Name="RestaurarWebAppToggle" Header="Restaurar pasta do WebApp"/>
                                    <ToggleSwitch x:Name="InstalarArtefatosToggle" Header="Instalar Artefatos (wes.exe)" IsOn="False"/>
                                </StackPanel>
                            </Border>

                            <!-- CARD 2 -->
                            <Border Style="{StaticResource ResponsiveCardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Configuração da Aplicação Web" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                    <TextBox x:Name="EnderecoServidorTextBox" Header="Endereço do Benner Server" Text="BNU-RHVAPI001" />
                                    <TextBox x:Name="UsuarioInternoTextBox" Header="Nome de usuário interno" Text="sysdba" />
                                    <PasswordBox x:Name="SenhaInternoPasswordBox" Header="Senha do usuário interno" />
                                    <Button x:Name="AbrirNavegadorButton" Content="Abrir aplicativo no navegador" HorizontalAlignment="Left" Margin="0,10,0,0" Click="AbrirNavegadorButton_Click"/>
                                </StackPanel>
                            </Border>

                            <!-- CARD 3 -->
                            <Border Style="{StaticResource ResponsiveCardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Outras Informações" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                                    <ComboBox x:Name="SiteComboBox" Header="Site" MinWidth="200" HorizontalAlignment="Stretch" IsEditable="True"/>
                                    <TextBox x:Name="NomeAplicacaoTextBox" Header="Nome da aplicação" Text="ESPRH_CAJ_DESENV"/>
                                    <ComboBox x:Name="PoolComboBox" Header="Pool" MinWidth="200" HorizontalAlignment="Stretch" IsEditable="True"/>
                                    <TextBox x:Name="NumeroProvedoresTextBox" Header="Nº de Provedores" Text="2"/>
                                    <TextBox x:Name="NomeSistemaBennerTextBox" Header="Nome do sistema no Benner Server" Text="ESPRH_CAJ_DESENV"/>
                                </StackPanel>
                            </Border>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- CARD 4: Terminal de Processamento -->
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,4">
                        <Button Content="Limpar Terminal" Click="BtnLimparLog_Click" FontSize="12" Padding="8,4"/>
                    </StackPanel>
                    <Border Grid.Row="1" Background="Black" CornerRadius="4" Padding="8">
                        <ScrollViewer x:Name="TerminalScrollViewer" Height="220" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="TerminalOutput"
                                       Text="Aguardando execução..." 
                                       FontFamily="Consolas" 
                                       Foreground="White" 
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Top"/>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!-- BOTÃO DE AÇÃO PRINCIPAL E PROGRESSO -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12" Margin="0,12,8,0">
                    <ProgressRing x:Name="LoadingRing" IsActive="False" Width="24" Height="24" VerticalAlignment="Center"/>
                    <Button x:Name="ProcessarButton" 
                            Content="PROCESSAR MONTAGEM DE AMBIENTE"
                            Style="{ThemeResource AccentButtonStyle}"
                            Click="ProcessarButton_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
