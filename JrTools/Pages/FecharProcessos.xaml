<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.FecharProcessos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:models="using:JrTools.Models"
    xmlns:dto="using:JrTools.Dto"
    xmlns:services="using:JrTools.Services"
    xmlns:converters="using:JrTools.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:JrTools.ViewModels"
    x:Name="RootPage"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <Style x:Key="ResponsiveCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="320"/>
            <Setter Property="MaxWidth" Value="5000"/>
        </Style>

        <Style x:Key="FullWidthCardStyle" TargetType="Border" BasedOn="{StaticResource ResponsiveCardStyle}">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- Template para o Botão individual de Processo -->
        <DataTemplate x:Key="ProcessButtonTemplate" x:DataType="viewmodels:ProcessViewModel">
            <ToggleButton 
                IsChecked="{x:Bind IsEnabled, Mode=TwoWay}"
                FontSize="16"
                Margin="10"
                Width="150" Height="150"
                HorizontalContentAlignment="Center" 
                VerticalContentAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{x:Bind NameDisplay}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                    <TextBlock Text="{x:Bind Count, Mode=OneWay}" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </ToggleButton>
        </DataTemplate>

        <!-- Template para o Botão Master -->
        <DataTemplate x:Key="MasterButtonTemplate" x:DataType="viewmodels:ProcessViewModel">
            <ToggleButton 
                IsChecked="{Binding DataContext.IsAutoKillEnabled, ElementName=RootPage, Mode=TwoWay}"
                FontSize="16"
                Margin="10"
                Width="150" Height="150"
                Foreground="White"
                HorizontalContentAlignment="Center" 
                VerticalContentAlignment="Center">
                <TextBlock 
                   Text="{Binding NameDisplay}"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"/>
            </ToggleButton>
        </DataTemplate>

        <local:ProcessCardTemplateSelector x:Key="CardSelector"
            MasterTemplate="{StaticResource MasterButtonTemplate}"
            ProcessTemplate="{StaticResource ProcessButtonTemplate}" />
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel Spacing="16">

                <TextBlock Text="Processos" Style="{ThemeResource TitleTextBlockStyle}" />

                <Border Style="{StaticResource FullWidthCardStyle}">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Fechar Processos" FontWeight="Bold" FontSize="18"/>
                        
                        <!-- Lista Unificada de Botões -->
                        <ItemsControl x:Name="ProcessosItemsControl" 
                                      ItemsSource="{x:Bind ViewModel.MonitoredProcesses, Mode=OneWay}"
                                      ItemTemplateSelector="{StaticResource CardSelector}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="-1" ItemWidth="170" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- BANNER PÓS-DEPLOY -->
                <Border x:Name="DeployBanner"
                        Visibility="{x:Bind ViewModel.DeployRecovery, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource AccentAAFillColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="8,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.DeployRecovery.Titulo, Mode=OneWay}"
                                       FontWeight="SemiBold" FontSize="13"/>
                            <ItemsControl ItemsSource="{x:Bind ViewModel.DeployRecovery.Results, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="services:ProviderRecoveryResult">
                                        <TextBlock Text="{x:Bind Resumo}" FontFamily="Consolas" FontSize="12" Margin="0,2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                        <Button Grid.Column="1" Content="✕" VerticalAlignment="Top"
                                Click="FecharBannerDeploy_Click"
                                Background="Transparent" BorderThickness="0" FontSize="14"/>
                    </Grid>
                </Border>

                <!-- DETALHES -->
                <Expander Header="Detalhes BPrv230" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Linha superior: lista de providers | informações do provider -->
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" MinWidth="220"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Coluna esquerda: lista de providers -->
                            <StackPanel Spacing="8" Padding="0,8,0,0">
                                <TextBlock Text="Providers em execução" Foreground="Gray" FontSize="12"/>
                                <ListView x:Name="ProcessosDataGrid"
                                          ItemsSource="{x:Bind ViewModel.BPrv230Details, Mode=OneWay}"
                                          SelectionMode="Single"
                                          SelectionChanged="ProcessosDataGrid_SelectionChanged"
                                          MinHeight="180">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:ProcessInfo">
                                            <Grid Padding="4,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{x:Bind ProcessName}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{x:Bind CommandLine}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{x:Bind PID}" FontSize="11" Foreground="Gray" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <Button x:Name="MatarProcessoButton" Content="Encerrar selecionado"
                                        Click="MatarProcessoButton_Click" IsEnabled="False"
                                        Foreground="White" Background="Red" HorizontalAlignment="Stretch"/>
                            </StackPanel>

                            <!-- Coluna direita: informações do provider selecionado -->
                            <StackPanel Grid.Column="1" Spacing="8" Padding="0,8,0,0"
                                        Visibility="{x:Bind ViewModel.HasSelectedProvider, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Informações do Provider" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <ComboBox Grid.Column="1" x:Name="LogTypeCombo"
                                              SelectedIndex="0"
                                              SelectionChanged="LogTypeCombo_SelectionChanged">
                                        <ComboBoxItem Content="BDebug (Tudo)"/>
                                        <ComboBoxItem Content="BDebug (Slice)"/>
                                        <ComboBoxItem Content="Provider Info"/>
                                    </ComboBox>
                                </Grid>

                                <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="6" Padding="10">
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedProviderInfo, Mode=OneWay}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="dto:ProviderInfoItem">
                                                <Grid Padding="0,3" ColumnSpacing="8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="160"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{x:Bind Description}" Foreground="Gray" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Grid.Column="1" Text="{x:Bind Value, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>

                            <!-- Placeholder coluna direita -->
                            <Border Grid.Column="1" Padding="16"
                                    Visibility="{x:Bind ViewModel.HasSelectedProvider, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <TextBlock Text="Selecione um provider para ver os detalhes."
                                           Foreground="Gray" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <!-- Linha inferior: log full width -->
                        <StackPanel Grid.Row="1" Spacing="6"
                                    Visibility="{x:Bind ViewModel.HasSelectedProvider, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Log em tempo real" Foreground="Gray" FontSize="12" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="Exportar .txt"
                                        Click="ExportarLogsButton_Click" FontSize="12"/>
                            </Grid>
                            <TextBox x:Name="ProviderLogOutput"
                                     Text="{x:Bind ViewModel.SelectedProviderLog, Mode=OneWay}"
                                     FontFamily="Consolas" FontSize="11"
                                     TextWrapping="Wrap"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Height="220"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0"
                                     Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                     TextChanged="ProviderLogOutput_TextChanged"/>
                        </StackPanel>
                    </Grid>
                </Expander>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
