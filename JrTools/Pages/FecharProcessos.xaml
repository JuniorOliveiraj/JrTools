<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.FecharProcessos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Estilo para os cards que se ajustam em grade -->
        <Style x:Key="ResponsiveCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="320"/>
            <Setter Property="MaxWidth" Value="500"/>
        </Style>

        <!-- Novo estilo para o card de terminal, que ocupa a largura total -->
        <Style x:Key="FullWidthCardStyle" TargetType="Border" BasedOn="{StaticResource ResponsiveCardStyle}">
            <Setter Property="MaxWidth" Value="9999"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- Novo estilo para Expander que ocupa largura total -->
        <Style x:Key="FullWidthExpanderStyle" TargetType="Expander">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel Spacing="16">

                <!-- Contêiner para título e botões -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Título -->
                    <TextBlock Text="Buildar Projetos" 
                        Style="{ThemeResource TitleTextBlockStyle}" 
                        VerticalAlignment="Center"
                        Grid.Column="0"/>


                </Grid>

                <InfoBar x:Name="ValidationInfoBar" IsOpen="False" Severity="Error" Margin="0,0,8,0"/>

                <!-- EXPANDER BUILDAR DOTNET -->
                <Expander  x:Name="ExpanderDotNet"  Style="{StaticResource FullWidthExpanderStyle}" IsExpanded="True">
                    <Expander.Header>
                        <TextBlock Text="Fechar Processos" VerticalAlignment="Center"/>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Spacing="12" HorizontalAlignment="Stretch">
                            <Border Style="{StaticResource FullWidthCardStyle}">
                                <StackPanel Spacing="12">
                                    <ItemsControl>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid MaximumRowsOrColumns="5" Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ToggleButton x:Name="ManterTudoFechadoToggleButton"
                                          Content="Manter Tudo Fechado"
                                          Click="ManterTudoFechadoToggleButton_Click"
                                          FontSize="16"
                                          Margin="10"
                                          Width="200" Height="200"
                                          HorizontalContentAlignment="Center" 
                                          VerticalContentAlignment="Center"/>
                                        
                                        
                                        <ToggleButton x:Name="BPrv230ToggleButton"
                                          Content="BPrv230"
                                          FontSize="16"
                                          Margin="10"
                                          Width="200" Height="200"
                                          IsChecked="True"
                                          HorizontalContentAlignment="Center" 
                                          VerticalContentAlignment="Center"/>
                                        <ToggleButton x:Name="CS1ToggleButton"
                                          Content="CS1"
                                          FontSize="16"
                                          Margin="10"
                                          Width="200" Height="200"
                                          IsChecked="True"/>
                                        <ToggleButton x:Name="BuilderToggleButton"
                                          Content="Builder"
                                          FontSize="16"
                                          Margin="10"
                                          Width="200" Height="200"
                                          HorizontalContentAlignment="Center" 
                                          VerticalContentAlignment="Center"/>
 
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- CARD 3: Terminal de Processamento -->
                            <Border Style="{StaticResource FullWidthCardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Terminal de Processamento" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                    <Border Background="Black" CornerRadius="4" Padding="8">
                                        <ScrollViewer x:Name="TerminalScrollViewer" Height="150" VerticalScrollBarVisibility="Auto">
                                            <TextBlock x:Name="TerminalOutput"
                                                Text="Aguardando execução..." 
                                                FontFamily="Consolas" 
                                                Foreground="White" 
                                                TextWrapping="Wrap"
                                                VerticalAlignment="Top"
                                                IsTextSelectionEnabled="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander.Content>

                </Expander>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>
