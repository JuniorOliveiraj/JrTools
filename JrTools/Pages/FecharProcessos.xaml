<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JrTools.Pages.FecharProcessos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JrTools.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"

    IsTabStop="True"
    AllowFocusOnInteraction="True"
    TabNavigation="Cycle">

    <Page.Resources>
        <Style x:Key="ResponsiveCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="320"/>
            <Setter Property="MaxWidth" Value="500"/>
        </Style>

        <Style x:Key="FullWidthCardStyle" TargetType="Border" BasedOn="{StaticResource ResponsiveCardStyle}">
            <Setter Property="MaxWidth" Value="9999"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style x:Key="FullWidthExpanderStyle" TargetType="Expander">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel Spacing="16">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Processos" 
                        Style="{ThemeResource TitleTextBlockStyle}" 
                        VerticalAlignment="Center"
                        Grid.Column="0"/>
                </Grid>

                <InfoBar x:Name="ValidationInfoBar" IsOpen="False" Severity="Error" Margin="0,0,8,0"/>

 
                        <TextBlock Text="Fechar Processos" VerticalAlignment="Center"/>
                        <StackPanel Spacing="5" HorizontalAlignment="Stretch">
                            <Border Style="{StaticResource FullWidthCardStyle}">
                                <StackPanel Spacing="5">
                                    <ItemsControl x:Name="ProcessosItemsControl">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid MaximumRowsOrColumns="5" Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ToggleButton x:Name="ManterTudoFechadoToggleButton"                                              
                                              Click="ManterTudoFechadoToggleButton_Click"
                                              FontSize="16"
                                              Margin="10"
                                              Width="150" Height="150"
                                              HorizontalContentAlignment="Center" 
                                              VerticalContentAlignment="Center">
                                            <TextBlock 
                                                Text="Manter Tudo Fechado"
                                                TextAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextWrapping="Wrap"/>
                                        </ToggleButton>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- DETALHES DOS PROCESSOS -->
                            <Expander Header="Detalhes dos Processos BPrv230"
                                      Style="{StaticResource FullWidthExpanderStyle}"
                                      IsExpanded="True">
                                <Border Style="{StaticResource FullWidthCardStyle}">
                                    <StackPanel Spacing="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Spacing="4">
                                                <TextBlock Text="Clique em um processo para ver os logs completos no terminal abaixo" 
                                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource SystemColorGrayTextColor}"/>
                                                <TextBlock Text="Pressione DELETE ou clique no botão para encerrar o processo selecionado" 
                                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource SystemAccentColor}"
                                                           FontWeight="SemiBold"/>
                                            </StackPanel>

                                            <Button Grid.Column="1" 
                                                    x:Name="MatarProcessoButton"
                                                    Content="Encerrar Processo"
                                                    Click="MatarProcessoButton_Click"
                                                    Background="{ThemeResource SystemFillColorCriticalBrush}"
                                                    IsEnabled="False"
                                                    VerticalAlignment="Center"/>
                                        </Grid>

                                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                            <ListView x:Name="ProcessosDataGrid"
                                                      IsItemClickEnabled="False"
                                                      SelectionMode="Single"
                                                      SelectionChanged="ProcessosDataGrid_SelectionChanged"
                                                      MinHeight="200">
                                                <ListView.Header>
                                                    <Grid Padding="8,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="120"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="120"/>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="100"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="Nome" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="1" Text="PID" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="2" Text="Host" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="3" Text="Tempo CPU" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="4" Text="Hora Início" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="5" Text="Versão" FontWeight="Bold"/>
                                                        <TextBlock Grid.Column="6" Text="Linha de Comando" FontWeight="Bold"/>
                                                    </Grid>
                                                </ListView.Header>
                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Padding="8,4">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="120"/>
                                                                <ColumnDefinition Width="80"/>
                                                                <ColumnDefinition Width="150"/>
                                                                <ColumnDefinition Width="120"/>
                                                                <ColumnDefinition Width="150"/>
                                                                <ColumnDefinition Width="100"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding NomeProcesso}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding PID}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="2" Text="{Binding Host}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="3" Text="{Binding TempoCPU}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="4" Text="{Binding HoraInicio}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="5" Text="{Binding VersaoArquivo}" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="6" Text="{Binding LinhaComando}" 
                                                                       TextWrapping="NoWrap" 
                                                                       TextTrimming="CharacterEllipsis"
                                                                       VerticalAlignment="Center"
                                                                       ToolTipService.ToolTip="{Binding LinhaComando}"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </Expander>

                            <!-- TERMINAL DE PROCESSAMENTO -->
                            <Border Style="{StaticResource FullWidthCardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Terminal de Processamento" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                    <TextBlock Text="Selecione um processo acima para ver seus logs detalhados" 
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource SystemColorGrayTextColor}"/>
                                    <Border Background="Black" CornerRadius="4" Padding="8">
                                        <ScrollViewer x:Name="TerminalScrollViewer" Height="300" VerticalScrollBarVisibility="Auto">
                                            <TextBlock x:Name="TerminalOutput"
                                                Text="Aguardando execução..." 
                                                FontFamily="Consolas" 
                                                Foreground="White" 
                                                TextWrapping="Wrap"
                                                VerticalAlignment="Top"
                                                IsTextSelectionEnabled="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
    

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>